1. Install typescript

npm install -g typescript

2. creare a new folder as ts-hello and change directory to that folder

3. create a ts file and open in vbs using the below command

code main.ts

4. To traspile(compile) ts file to javascipt using the below command

tsc filename.ts
 
5. To execute this ts file type 
node filename.js


6. In typescript use let instead of var in declaraion just for code optimization even though the file will get compile to js and change let to var and even u can run the js file. (this is just to  identify the error before itself


7. Declare type of variable as below

let a: number, boolean, string, any, number[]
